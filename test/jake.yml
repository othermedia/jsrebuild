---
source_directory: source
build_directory:  build
layout:           together

builds:
  source:
    suffix:       false
    packer:       false
  min:
    shrink_vars:  true
    private:      true

packages:
  fake:
    files:
      - fake
      - faker
